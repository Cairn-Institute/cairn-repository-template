name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: echo "Add install command here"
      - name: Format check
        run: echo "Add format check command here"
      - name: Lint
        run: echo "Add lint command here"
      - name: Test
        run: echo "Add test command here"
